const initialState = {
    api: {
        url: 'sp19-cs411-52.cs.illinois.edu'
    }
};

const AppReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default AppReducer;
